# MCP CLI Test Commands

# List all available tools
mcp list-tools --url http://localhost:8000

# Call the hello_world tool
mcp run-tool hello_world --url http://localhost:8000 --args '{"name": "CLI User"}'

# Get tool schema
mcp get-tool-schema hello_world --url http://localhost:8000